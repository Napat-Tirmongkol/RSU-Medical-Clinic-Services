# hot.md — Project Snapshot

> **Startup cache สำหรับ AI agent** — อ่านไฟล์นี้เป็นไฟล์แรกของทุก session ก่อนทำงานใหม่
> เป้าหมาย: ให้ agent เข้า working mode ได้ภายในไม่กี่วินาที โดยไม่ต้อง warm-up นาน
>
> **Update คู่กับ commit สำคัญทุกครั้ง** — ถ้าเปลี่ยน phase / ปิด task ใหญ่ / decision สำคัญ → แก้ที่นี่ทันที
> ใช้ภาษาธรรมชาติ ไม่ใช่ checklist — เขียนให้คนใหม่อ่านแล้วเข้าใจสถานะภายใน 60 วินาที

---

## Project ใจความ
**RSU Medical Clinic Services** — ระบบบริหารคลินิกแพทย์ของมหาวิทยาลัยรังสิต ครอบคลุม
ประกันสุขภาพ · นัดหมายแพทย์ · ครุภัณฑ์/วัสดุสิ้นเปลือง (e_Borrow + asset + consumables) · การเงิน (Cash Book) · LINE webhook · AI assistant (Gemini) · ISO 27001 audit · EDMS

Stack: PHP 8 + MySQL (PDO singleton) · Tailwind compiled CSS (no JIT) · vanilla JS · Chart.js · SweetAlert2 · Driver.js (tour) · Font Awesome

---

## สถานะปัจจุบัน (พฤษภาคม 2026)

### Phase ที่จบล่าสุด
- **Scholarship Payout Status (2026-05-26, branch `claude/fervent-knuth-rloqy`)** — เพิ่มสถานะการจ่ายเงินรายเดือนให้นักศึกษาทุน เพื่อ track ว่าใคร "รอดำเนินการการเงิน" / "การเงินอนุมัติ (พร้อมรับ)". **Schema** ตารางใหม่ `sys_scholarship_payouts` (snapshot ต่อ student × month: `period_ym CHAR(7)`, `hours_paid`, `pay_rate`, `amount`, `status ENUM('pending','approved')`, `generated_by/at`, `approved_by/at`, `note` · UNIQUE `(student_id, period_ym)` · ตาราง auto-migrate ผ่าน `ensure_scholarship_schema()`). **Helper** เพิ่ม 5 ฟังก์ชันใน `includes/scholarship_helper.php`: `scholarship_period_range()` / `scholarship_period_thai()` / `generate_scholarship_payouts()` (idempotent — pending records update snapshot, approved frozen) / `list_scholarship_payouts()` / `scholarship_payout_summary()` / `set_scholarship_payout_status()`. **AJAX** entity `payouts` ใน `portal/ajax_scholarship.php`: list (filter status + search) / generate / approve / unapprove / bulk_approve (transactional) / update_note / delete (เฉพาะ pending) / export_csv (early-return ก่อน JSON header). **UI** tab ใหม่ "การจ่ายเงิน" (icon `fa-money-check-dollar`, badge นับ pending ของเดือนปัจจุบัน) ระหว่าง "รายงาน" กับ "ตั้งค่า" · period picker (month input) + ปุ่ม สร้าง/อัปเดตรายการ · 3 KPI tiles (รอดำเนินการ + ยอด / อนุมัติแล้ว + ยอด / รวมทั้งเดือน) · filter chips (ทั้งหมด/รอ/อนุมัติ) · search debounce 350ms · sortable table (รอ → อนุมัติแล้ว) พร้อม per-row actions (อนุมัติ / แก้หมายเหตุ / ลบ) สำหรับ pending และ ปุ่ม "ย้อนกลับ" สำหรับ approved · bulk approve checkboxes เฉพาะแถว pending · pagination 20/page · export CSV (UTF-8 BOM). **Workflow**: end-of-month admin คลิก "สร้าง/อัปเดตรายการ" → ระบบคำนวณ hours_paid × pay_rate ของแต่ละนักศึกษา active → INSERT status='pending' → การเงินอนุมัติทีละคน หรือ bulk → status='approved' พร้อมให้นักศึกษามารับ. **Decision**: ไม่ sync เข้า Cash Book อัตโนมัติ (มีปุ่ม "ส่งเข้า Cash Book" ใน Dashboard tab อยู่แล้วสำหรับยอดรวม) — ตารางนี้ track per-student status เท่านั้น. **Snapshot pattern**: เก็บ `pay_rate` + `amount` ใน row ตอน generate เพื่อ freeze ตอน approve แล้ว (ถ้ารายการ approve แล้ว แต่ admin เปลี่ยน rate ภายหลัง — ยอดเก่ายังคงเดิม). Files: `includes/scholarship_helper.php`, `portal/ajax_scholarship.php`, `portal/_partials/scholarship.php`
- **LINE Chat Phase 2 — Productivity + Workflow + Rich Content (2026-05-25, branch `claude/nifty-shannon-1ndI2`)** — ยกระดับ LINE Chat ใน portal จาก Phase 1 MVP เป็นเครื่องมือทำงานจริงของแอดมินคลินิก. **Sidebar restructure**: ย้าย LINE Chat จากกลุ่ม AI Suite ออกมาเป็นกลุ่ม "LINE Official" ใหม่ (data-group `line`, สี #06c755) — รองรับเมนู LINE OA ในอนาคต. **2 ตารางใหม่** (`sys_line_conversations` per-convo state + `sys_line_reply_templates` quick replies) auto-migrate. **Productivity**: (1) Search box ใน convo list — debounce 350ms ค้น display_name/uid/last_msg/tags · (2) AI suggested reply (Gemini 2.5-flash) — ปุ่ม "AI ช่วยร่าง" call `line_chat_suggest_reply()` ดึงประวัติ 20 ข้อความ + system prompt ภาษาไทย/สุภาพ/ไม่แต่งข้อมูล → เติมใน textarea + toast เตือน "ตรวจก่อนกดส่ง" (LLM hardening: separate role turns กัน prompt injection) · (3) Quick reply templates — popover เลือก template พร้อม search + group by category, manager modal CRUD เต็ม (add/edit/delete/toggle active), use_count tracking. **Workflow**: (4) Mark as resolved — ปุ่มในหัว convo + filter chip "ปิดเคสแล้ว" + opacity-65 ใน list + emerald badge "ปิดเคสแล้ว" · admin reply อัตโนมัติ un-resolve · (5) Tags chips — right side panel เพิ่ม/ลบ tag (max 10/convo × 30char) + preset 6 ตัว (นัดหมาย/ประกัน/สอบถามทั่วไป/ร้องเรียน/VIP/ติดตามผล) · ใช้ใน search filter ได้ · (6) Internal notes — textarea ใน side panel (5000 char) + แสดง "อัพเดต X นาทีที่แล้ว โดย Y". **Rich content (display only)**: (7) Webhook capture inbound sticker/image/location/file/audio/video — เก็บ JSON metadata ใน `message_text` พร้อม `message_type` column · (8) UI render bubbles: sticker → LINE CDN URL, image → proxy via `line_media_proxy.php`, location → Google Maps link, file/audio/video → download link with icon · (9) **`line_media_proxy.php`** — auth-gated content proxy ที่ดึงจาก LINE Content API → cache `uploads/line_media/YYYY/MM/sha256.ext` พร้อม strict MIME whitelist (jpg/png/gif/webp/pdf/m4a/aac/mp4) ไม่เคย echo raw header, CSP sandbox, .htaccess deny-all root, validate msg_id format + DB-lookup ก่อนยิง LINE API. **DB performance**: refactor `line_chat_list_conversations()` ใช้ `MAX(id)` self-join แทน 3 correlated subqueries → 1 PK seek/row, conditional `LEFT JOIN lm` ใน count query (skip ถ้าไม่ search). **UI polish**: relative timestamps ("5 นาทีที่แล้ว"), char counter (warn เมื่อ > 3500/4000), preset tags, dark mode ครอบทุก surface ใหม่, Portal-Escape modals z-9000. รีวิว security ผ่าน HIGH 2 จุด (raw MIME pass-through → whitelist enforce, glob() wildcard → iterate whitelist) + DB P1 (correlated subq → MAX(id) join). Files: `_partials/line_chat.php`, `ajax_line_chat.php`, `line_media_proxy.php` (new), `includes/line_chat_helper.php`, `api/line_webhook.php`, `_layout_top.php` (sidebar), `assets/css/portal.css` (line accent), `CLAUDE.md`
- **Payroll ไทย — ครบ lane (2026-05-25, branch `claude/festive-dijkstra-JoMHd`)** — โมดูล payroll สำหรับคลินิกตามมาตรฐาน Thai PIT 2024-2026 (ภงด.1 + ประกันสังคม + provident fund + payslip). **3 ตารางใหม่** (`sys_payroll_employees`, `sys_payroll_periods`, `sys_payroll_entries`) + auto-add `sys_staff.access_payroll` flag. **Calc engine** ที่ `pr_calc_entry()`: gross = base + allowance + (ot_hours × ot_rate) + bonus + other_income · SSO = min(gross × 5%, 750) ต่อเดือน (ลูกจ้าง+นายจ้างเท่ากัน) · PF = gross × rate% (optional per employee) · **PIT monthly withholding**: annualize × 12 → หัก 40% expense (cap 100,000/ปี) − allowances (personal 60k + spouse + 30k/บุตร) − annual SSO (cap 9,000) → progressive brackets 0/5/10/15/20/25/30/35% → /12 เป็น monthly tax. **Schema invariants**: period.status `draft → approved → paid` (entries lock เมื่อ != draft), entry update FOR UPDATE + status re-check, period.UNIQUE on period_ym, entry.UNIQUE on (period_id, employee_id). **AJAX** `portal/ajax_payroll.php` entity:action (employee/period/entry/lookup/report) · `report:*` (pnd1_csv, sso_csv, bank_csv) early-return ก่อน JSON header สำหรับ stream CSV. **CSRF gate**: read-only verbs (list/get) + read-only entities (lookup) exempt, mutating actions ต้อง POST + CSRF token. **Cash Book sync**: `pr_mark_period_paid()` lock period FOR UPDATE → `finance_sync_upsert(source_module='payroll_period', source_id=period.id, kind='expense', category='เงินเดือน/ค่าจ้าง')` → fail = rollback ทั้งหมด. **3 หน้า UI**: `payroll_employees.php` (Portal-Escape modal + staff typeahead + 5 sections form: identity/salary/SSO+PF/tax allowances/bank · auto-disable picker on edit) · `payroll_periods.php` (list + Portal-Escape **detail modal มี editable entries** — ช่อง OT/bonus/หักอื่นๆ แก้ได้ inline เฉพาะ draft, save row → recalc กับ helper → update display ทันที + refresh totals · 3 ปุ่ม export CSV (ภงด.1/ประกันสังคม/Bank Transfer) · ปุ่ม approve/unapprove/pay/cancel/delete พร้อม Swal confirm) · `payroll_payslip.php` standalone A4 print-friendly (gradient brand-green header + 2-column income/deduction box + giant net pay box + bank details + employer-side SSO/PF note). **Sidebar**: เพิ่ม 2 menu "Payroll · พนักงาน" และ "Payroll · งวดเงินเดือน" ใต้ "การเงิน" group → ตอนนี้ group มี 7 items. **Phase 2 จบ → ERP lane plan ครบ 2 lane (Patient Billing + Payroll)**. Pattern ที่ใช้ซ้ำ: schema ensure + finance_sync_upsert idempotent + Portal-Escape modal + entity:action AJAX + status state machine + readonly UI on locked states
- **Patient Billing Phase 1D — AR Aging Dashboard (2026-05-25, branch `claude/festive-dijkstra-JoMHd`)** — ปิด lane Patient Billing! รายงานลูกหนี้คงค้างแบบ standard accounting 4-bucket aging (0-30 / 31-60 / 61-90 / 90+ วัน). **Helper** เพิ่ม `pb_ar_aging_summary($pdo, $payerType=null)` คำนวณ age จาก `COALESCE(due_date, issue_date) → CURDATE()` แล้วแยกเข้า bucket + คำนวณ summary (count/total/avg_age/oldest_invoice) ใน single pass · `pb_ar_aging_top_patients($pdo, $limit=10)` group by patient_id เรียง balance DESC · `pb_ar_aging_detail($pdo, $bucket, $payerType, $limit=200)` รายการใบในช่วงนั้น. Filter เฉพาะ status IN ('issued','partially_paid','overdue') AND balance > 0 — ไม่ติด paid/void. **AJAX** `aging:summary / detail` (JSON) + `aging:export` (stream CSV bypass JSON header — early-return ใน ajax_billing.php). **UI** `portal/_partials/billing_ar_aging.php` (~440 LOC): 4 KPI tiles (ยอดคงค้างรวม/อายุเฉลี่ย/ใบเก่าสุด/เลย 90 วัน) · **4 bucket cards คลิกได้** (มี color coding green→amber→orange→red ตาม urgency, active state highlight ด้วย border-emerald + shadow) · **Top 10 patients** card (left col, อันดับ 1 มี 🏆 + ยอดสีแดง) · **Drill-down table** (right col 2/3 width) แสดงรายการใบในช่วงที่เลือก พร้อม age pill สีตาม bucket + ลิงก์ "รับชำระ" ไป billing_invoices.php?focus_invoice=N · filter ประเภทผู้ชำระ (patient/insurance/gold_card/other) · ปุ่มดาวน์โหลด CSV (BOM UTF-8 พร้อม header + footer รวมยอด) · auto-refresh ทุก 60s (visibility-aware skip เมื่อ tab hidden). **Sidebar** เพิ่ม "ลูกหนี้คงค้าง (AR)" ใต้ "การเงิน" → group นี้มี 4 menu: Cash Book / บริการคลินิก / บันทึกการเข้ารับบริการ / ใบแจ้งหนี้ & รับชำระ / ลูกหนี้คงค้าง (AR). **Insurance bridge note**: ตอนนี้รองรับเป็น `payer_type='insurance'` filter ใน aging report (เห็นยอดคงค้างจากประกันแยกได้) แต่ยังไม่ link FK ตรงเข้า `insurance_companies.company_code` (VARCHAR PK) เพราะ payer_id เป็น INT — phase ถัดไป (ถ้าจำเป็น) ค่อยขยายเป็น polymorphic FK หรือใช้ `payer_code` column. **Lane Patient Billing จบครบ 4 phase!** Workflow ที่ admin ทำได้ตอนนี้: Service Catalog → Encounter → Invoice → Payment → Cash Book sync → AR Aging tracking. Next lane: **Payroll ไทย** (เงินเดือน + OT + ภงด.1 + ประกันสังคม + provident fund + payslip)
- **Patient Billing Phase 1C — Invoice + Payment + Cash Book Auto-sync (2026-05-25, branch `claude/festive-dijkstra-JoMHd`)** — ปิด workflow ของ Patient Billing AR: finalized encounter → ใบแจ้งหนี้ → รับชำระ → เข้า Cash Book อัตโนมัติ. **Helper** เพิ่ม `pb_next_invoice_no()` (`INV-{yearBE}-{seq:06d}` + retry on UNIQUE collision), `pb_generate_invoice_from_encounter()` (atomic — lock encounter FOR UPDATE, refuse ถ้า status != 'finalized' หรือ encounter นี้มีใบแล้ว, copy ยอดจาก encounter, flip status → 'invoiced'), `pb_list_invoices()` (filter status/payer_type/date range/patient), `pb_get_invoice_full()` (header + encounter info + line items snapshot + payment history), `pb_record_payment()` (transaction-safe: lock invoice → insert payment → update paid_amount + status (paid/partially_paid auto-detect) → `finance_sync_upsert(source_module='billing_payment', source_id=payment.id, category='ค่ารักษาผู้ป่วย')` → link `finance_txn_id` กลับใน payment row · **consistency-first**: ถ้า Cash Book sync fail → rollback ทั้งหมด ไม่ให้เกิด payment ที่ Cash Book มองไม่เห็น). Constants: `PB_DEFAULT_INCOME_CATEGORY = 'ค่ารักษาผู้ป่วย'` (match seed ใน ajax_finance.php). **AJAX** ขยาย entity `invoice:*` (list/get/create_from_encounter/void) + `payment:*` (create). Void refuse ถ้ามี payment แล้ว (refund flow ทำใน Phase 1D). **Encounter UI** เพิ่มปุ่ม "ออกใบแจ้งหนี้" (ฟ้า, fa-file-invoice-dollar) บน finalized encounters → SweetAlert ขอ payer_type + due_date (default = today+30d) → call invoice:create_from_encounter → success ส่งลิงก์ไป billing_invoices.php?focus_invoice=N · status 'invoiced' โชว์ปุ่ม "ไปดูใบแจ้งหนี้" แทน. **UI ใหม่** `portal/_partials/billing_invoices.php` (~720 LOC): filter bar (search/status/payer_type/date), table แสดง invoice_no/วันที่/ผู้ป่วย/ผู้ชำระ/ยอด/ชำระแล้ว/คงค้าง/สถานะ+action, Portal-Escape detail modal (z-9000) ที่มี receipt-style header (gradient brand green), invoice items snapshot, totals box (subtotal/discount/tax/total/paid/balance), payment history list (สี emerald + เลข txn ใน Cash Book), inline payment form (amount max = balance, method dropdown, date, ref, note), void button (เฉพาะใบที่ยังไม่มี payment), print button (`@media print` rules hide chrome). **URL focus params**: `?focus_invoice=N` auto-open modal · `?focus_encounter=N` filter list. **Sidebar** เพิ่ม "ใบแจ้งหนี้ & รับชำระ" ใต้ "การเงิน" group. Next: **Phase 1D** AR Aging report (0-30/31-60/61-90/90+) + Insurance Hub bridge
- **Patient Billing Phase 1B — Encounter UI (2026-05-25, branch `claude/festive-dijkstra-JoMHd`)** — บันทึกการเข้ารับบริการ (visit) เต็ม stack. **AJAX** ขยาย `portal/ajax_billing.php` รับ entity ใหม่ `encounter:*` (list/get/save/finalize/cancel/delete) และ `lookup:*` (patient/provider/service typeahead). **Helper** เพิ่ม `pb_next_encounter_no()` (format `ENC-{yearBE}-{seq:06d}` retry 3 ครั้งบน UNIQUE collision), `pb_lookup_patient()` (search sys_users ด้วย name/student_id/phone/citizen_id), `pb_lookup_provider()` (sys_staff), `pb_recalc_encounter_totals()`, `pb_list_encounters()` (filter status/date_from/date_to/patient_id), `pb_validate_encounter_header()`. **UI** `portal/_partials/billing_encounters.php` (~900 LOC): filter bar (search/status/date range), table แสดง encounter_no+date+patient+provider+total+status+actions, Portal-Escape modal สำหรับ create/edit/view, **typeahead** สำหรับ patient + provider พร้อม debounce 250ms + click-outside-to-close, **line items table** ที่มี service `<select>` พร้อม `<optgroup>` ตาม category + quantity/price/discount/line_total auto-calc, totals panel แสดง subtotal/discount/tax/total real-time, status-aware UI (draft = editable, finalized/invoiced/cancelled = readonly), บันทึกได้ทั้ง "ฉบับร่าง" และ "ปิดงาน" (save + finalize ใน 1 คลิก) ผ่าน chained AJAX. **Schema invariants**: encounter `status='draft'` แก้ไขได้ทุกอย่าง, `finalized` lock + เปิดทาง invoice ใน Phase 1C, `invoiced` lock เพราะมี downstream invoice, `cancelled` lock + ไม่ออกใบ. **Sidebar** เพิ่ม "บันทึกการเข้ารับบริการ" ใต้ "การเงิน" group ถัดจาก "บริการคลินิก". Next: **Phase 1C** invoice + payment + Cash Book sync
- **Patient Billing Phase 1A — Schema + Service Catalog (2026-05-25, branch `claude/festive-dijkstra-JoMHd`)** — เริ่ม "lane" ยกระดับเป็น ERP โดยเลือก 2 lane: Patient Billing (AR) ก่อน → Payroll ไทย. **Phase 1A** ครอบคลุม 5 ตาราง schema ใหม่ (`sys_billing_services`, `sys_billing_encounters`, `sys_billing_encounter_items`, `sys_billing_invoices`, `sys_billing_payments`) + service catalog CRUD เต็ม stack. **Schema**: services มี `code` UNIQUE, category enum 7 ค่า (consultation/treatment/procedure/lab/vaccination/consumable/other), unit_price + unit_label, is_taxable, linked_vaccine_type_id/linked_consumable_id (FK ในอนาคต). Encounter/invoice/payment สร้างไว้รอ Phase 1B-1C ใส่ UI. **Migration** `database/migrations/migrate_patient_billing.php` พร้อม seed 10 บริการตั้งต้น (GP/SP/CBC/urine/dressing/inject/flu/tetanus/IV/cert). **Helper** `includes/patient_billing_helper.php` (idempotent `pb_ensure_schema()` + `pb_list_services()` + `pb_validate_service_payload()` + 4 const maps). **AJAX** `portal/ajax_billing.php` entity:action (service: list/get/create/update/toggle/delete) ผ่าน CSRF + role gate (access_finance/admin/superadmin). **UI** `portal/_partials/billing_services.php` 600 LOC: filter bar (search/category/active), pagination 20/page, Portal-Escape modal สำหรับ add/edit, SweetAlert2 confirm/error, dark mode รองรับ. **Sidebar** เพิ่ม "บริการคลินิก" เข้าใต้ group "การเงิน" (ใช้ access_finance gate ร่วม Cash Book). Next: **Phase 1B** encounter UI (visit booking + service line items + auto-calc totals)
- **Walk-in รับ overflow ตอน status='full' (2026-05-24, branch `claude/festive-dijkstra-JoMHd`)** — เดิม Walk-in QR ถูกบล็อกทุกระดับเมื่อ `camp_list.status='full'` (toggle endpoint ปฏิเสธ enable, edit handler auto-disable, entry page/submit handler ปฏิเสธลงทะเบียน, UI แสดง warning "ระบบจะปฏิเสธการลงทะเบียนทั้งหมด"). แก้ให้ `'full'` เป็น valid status เทียบเท่า `'active'` สำหรับ Walk-in lane (เพราะ walk-in คือ overflow). ลบ campaign-level `used >= total_capacity` gate ทั้งใน `user/walkin.php` (line 89) และ `user/ajax_walkin_submit.php` (capacity re-check ใน transaction). **Slot-level capacity ยังบังคับ** (กันรอบเวลาล้น). แก้ 5 ไฟล์: `admin/ajax/ajax_toggle_walkin.php` (gate), `admin/campaigns.php` (auto-disable on edit + `$_walkinBlocked` UI flag), `admin/campaign_overview.php` (`$_coWalkinBlocked`), `user/walkin.php` (state machine), `user/ajax_walkin_submit.php` (submit guard + transaction re-check)
- **Walk-in QR Registration (2026-05-24, branch `claude/focused-knuth-Z9Hho`)** — ระบบลงทะเบียน Walk-in ผ่าน QR สำหรับแคมเปญ: `user/walkin.php` (entry page + flow guard: HMAC token → LINE auth → profile check → slot picker → confirm → `ajax_walkin_submit.php`) · `user/api_walkin_qr.php` (PNG QR generator) · `admin/walkin_poster.php` (A4 print-ready poster พร้อม html2pdf.js) · `admin/ajax/ajax_toggle_walkin.php` + `ajax_get_walkin_url.php` · ปุ่ม 🚶 ใน campaigns.php (amber modal · Portal-Escape z-9000) · ปุ่มใน campaign_overview.php · Schema: `camp_list.walkin_enabled TINYINT(1)` auto-migrate ทุก entry point · `camp_bookings.is_walk_in=1` compatible กับ admin/bookings.php badge · DB transaction พร้อม FOR UPDATE re-check (race-safe) · 8 error states ครบทุก branch
- **e-Campaign Admin Overhaul Phase 2 (2026-05-24, branch `claude/focused-knuth-Z9Hho`)** — design overhaul ของ `admin/time_slots.php` (ไฟล์ใหญ่สุดในระบบ admin 2021 LOC). Keep monolithic file architecture (architecture refactor เก็บไว้ session ถัดไป). **CSS block rewrite**: 150 → 600 LOC ครอบคลุม CSS variables (ใช้ `--ec-*` จาก shell), dark mode override ทุก surface, 18 new component classes (`.ts-modal*`, `.ts-input/select/label`, `.ts-btn-primary/amber/ghost`, `.ts-row-btn.qr/edit/del`, `.ts-cta`, `.ts-mini-btn`, `.ts-view-toggle`, `.ts-month-select`, `.ts-multi-trigger/dropdown/item`, `.ts-empty`, `.ts-field-card`, `.tone-full/near/ok` for capacity, `.slot-pal-emerald/green/purple/orange/red/teal` for campaign color rotation), Flatpickr dark-mode tweaks, simple-datatables theme integration. **Header_actions toolbar** rewrite: deleteMultiBtn → `.ts-danger-btn`, view toggle → `.ts-view-toggle`, month dropdown → `.ts-month-select`, add CTA → `.ts-cta`, multi-select dropdown → `.ts-multi-*` classes พร้อม fixed-position teleport (z-index 9001) ที่ฝัง logic เดิมไว้. **Slot card markup**: ลบ inline `style="background:#xxx;color:#xxx"` (capacity badges + slot-pal palette) → ใช้ `.tone-{full|near|ok}` และ `.slot-pal-{color}` classes ที่มี dark mode รองรับ. **3 modals** (slotModal/editSlotModal/dailyModal/qrModal): convert จาก `fixed inset-0 bg-gray-900/60` + `rounded-[24px]` + z-index 1000 → `.ts-modal` + `.ts-modal-box` + Portal-Escape pattern (`openTsModal()`/`closeTsModal()` helpers teleport ไป body + z-index 9000 + ป้องกัน body scroll). **JS helpers**: `openTsModal(id)`/`closeTsModal(id)` global helpers + ESC key dismiss + backdrop click dismiss. **Refactor JS string templates** (renderDailySlots, dailyEditRow, generateTimeSlots): replace hex literals กับ `var(--ec-*)` + use `stat-badge tone-*` + `ts-input` + `ts-cta` classes. **Security**: `htmlspecialchars()` → `json_encode()` ใน JS string args (bulkCancelSlot) กัน apostrophe break. **Lib cleanup**: ลบ duplicate SweetAlert2 CDN (shell preload แล้ว). **Antipatterns ลบครบ**: 0 occurrence ของ `bg-[#XXX]`, `rounded-[NN]`, `font-[NNN]`, `max-h-[XX]`, `max-w-[XX]` ใน markup. ดู `AI/logs/2026-05-24-ecampaign-time-slots-overhaul-phase2.md`
- **e-Campaign Admin Overhaul Phase 1 (2026-05-24, branch `claude/focused-knuth-Z9Hho`)** — ยกเครื่อง shell + dashboard ของ admin module ทั้งหมด (23 admin pages share `admin/includes/header.php`). **Shell rebuild** (`admin/includes/header.php` ~880 LOC): replace hex literals → CSS custom properties (`--ec-brand-50/100/.../700`, `--ec-acc-overview/campaign/report/tools`); complete dark-mode support via `body[data-theme='dark']` overrides for sidebar/topbar/notification panel/user pill/back-portal button; sidebar sections เปลี่ยนเป็น collapsible accordion (`.nav-section.collapsed`) มี per-section accent dot และ chevron toggle, state persist ใน `localStorage.ec_nav_collapsed`, auto-expand section ที่มี active nav-link; ลบ `font-black uppercase tracking-widest` (Thai ไม่ทำงาน) → `font-semibold/700`; `--lift` CSS variable pattern บน nav-link hover; SweetAlert2 CDN preloaded ใน head (ใช้ได้ทุก admin page); notification dropdown ใช้ visibility-aware polling (skip เมื่อ `document.hidden`); FOUC fix ด้วย body-script ที่ apply theme ทันทีหลัง body parse. **Dashboard rebuild** (`admin/index.php` ~900 LOC visible content): KPI tiles ใหม่ pattern (border-l-4 + slate-900 number + period delta vs เมื่อวาน/7วันก่อน — `+12% vs เมื่อวาน` chip มีสี dir-based up/down/flat); booking funnel bar 4-segment proportional (booked/confirmed/completed/cancelled) + per-segment legend with %; Chart.js 12-week trend bar (theme-aware ผ่าน `chartTheme()` + `MutationObserver` re-render on `data-theme` change); campaign type donut (vaccine/training/health_check/other) with center label; popular campaigns list คลิกได้ไปยัง `campaign_overview.php?id=N`; heatmap 24h×7d ใช้ data-light/data-dark dual-palette + theme switcher JS; polling 5s → 15s + visibility-aware (skip เมื่อ tab background); skeleton-style shimmer animation ใน `<style>` block (no RsuFx dependency). **AJAX** (`admin/ajax/ajax_dashboard.php`): refactor popular_html generator ให้ใช้ `.ec-popular-row` class ใหม่ + รักษา backward-compat fields (`stats.total/pending/confirmed/bookings_today/new_users_7d`). **Build**: rebuild `tailwind.min.css` (npm install + npm run build:css) เพื่อ compile `.ds-card`, `.ds-btn-primary`, `.ds-pill-brand` ที่ portal partials อื่นๆ ใช้ (ก่อนนี้ stale ไม่มี). Antipatterns ที่ลบ: hex literals ในมาร์กอัป (`bg-[#2e9e63]` → CSS var), arbitrary Tailwind values (`rounded-[18px]` → `border-radius:18px` ใน custom class), `font-[950]` (custom non-existent weight) → `font-weight:800`. ดู `AI/logs/2026-05-24-ecampaign-admin-overhaul-phase1.md` สำหรับ deep-dive
- **Portal Multi-Page Refactor (2026-05-23, branch `claude/adoring-babbage-anRYh`)** — แตก `portal/index.php` (6476 บรรทัด ไฟล์เดียว) เป็น 42 standalone page files ภายใต้ `portal/<section>.php` + shared layout system. **Architecture**: `_init.php` (auth+role flags+portal_handlers · 70 LOC) → `_layout.php` (orchestrator with `layout_start()`/`layout_end()` functions) → `_layout_top.php` (978 LOC: DOCTYPE→opening `<main>`) + `_layout_bottom.php` (2275 LOC: closing scripts+`</html>`) + `_portal_data.php` (621 LOC: KPI queries+activity log+announcement handler+maintenance — required by dashboard/announcements/identity/settings). **Sidebar links**: `<button onclick="switchSection('X')">` → `<a href="X.php">` (38 conversions via Python). **EDMS sub-menu**: `?section=edms&edms_view=Y` → `edms.php?view=Y` + compat shim ใน `edms.php` ที่ alias `view` → `edms_view`. **Backwards-compat**: `portal/index.php` (เหลือ 60 บรรทัด) เป็น redirect router — `?section=X` → 302 ไป `X.php` (bookmark/email/LINE deep-links ยังใช้ได้). **switchSection JS** เก็บไว้ใน `_layout_top.php` แต่ fallback ไป `window.location.href = sectionId + '.php'` เมื่อหา in-page target ไม่เจอ (กัน `<a href="javascript:switchSection('X')">` เก่าพัง). **CSS**: เพิ่ม `text-decoration: none` ที่ `.psb-item` เพราะตอนนี้เป็น `<a>` แล้ว. **Legacy files overwritten**: 9 standalone files เก่า (`activity_logs.php`, `apps.php`, `error_logs.php`, `insurance_sync.php`, `line_settings.php`, `nurse_schedule.php`, `profile.php`, `smtp_settings.php` — verified orphan: grep ทั้ง codebase ไม่มี reference). Log + Recipe: `AI/logs/2026-05-23-portal-multi-page-refactor.md`. Stats: 47 files created, 2 modified, 9 overwritten, ~900 net LOC reduction
- **UX Overhaul สำหรับ Staff ทั่วไป (2026-05-23, branch `claude/adoring-babbage-anRYh`)** — รวม 5 commits ผ่าน critique → priority/clarify/quieter/distill/onboard cycles. **คะแนน Nielsen heuristic ก่อน 22/40 → คาดหลัง ~30/40**. (1) **Plain Thai language pass** (`a4ab79a`, `b93cef1`) — replace tech jargon (TAT/SLA/Ack/breach/deadline/Superadmin) → ภาษาบ้าน (เวลาเฉลี่ยที่ใช้, เลยกำหนด, รับทราบ, ผู้ดูแลระบบ) ใน 5 partials (sla_dashboard, myinbox, sla_policies, detail, edms landing) + helper `sla_state_label()`. (2) **Empty states ที่บอกขั้นถัดไป** — filter-aware ใน My Inbox (open/warning/breached/paused/done) แต่ละสถานะมี hero icon + heading + sub-description + CTA button ที่ตรง context. (3) **Quieter clinical tone** (`a540953`) — ลบ `font-black uppercase tracking-widest` (ไม่ทำงานกับภาษาไทย) ทั่ว 3 partials หลัก → `font-semibold` · ลบ `fx-tilt fx-tilt-light` จาก KPI tiles · KPI pattern เปลี่ยน: border-{color}-200 → border-slate-200 + border-l-4 border-l-{color}-500 (calm accent stripe แทน outline เต็มสี) · ตัวเลขสี emerald-600 → slate-900 (less competing) · Finance: ลบ pastel bg + uppercase/letter-spacing · Header rainbow strip ลด 6→4 stops + slower 14s→22s + opacity 0.85. (4) **Priority Panel Hero pattern** — sort `$today_items` by tone (danger→warning→accent→info→success) แล้วดึง item เร่งสุด (danger หรือ warning) ออกมาแสดงเป็น `.priority-hero` ใหญ่มี pill "🔥 ทำอันนี้ก่อน" (CSS เพิ่มใน `assets/css/portal.css` รองรับ dark mode). (5) **Distill sidebar** — Default collapse 11/12 groups (เก็บแค่ overview) สำหรับ first-time visitor · เพิ่ม `#psb-search` input + Ctrl/Cmd+K shortcut · auto-expand groups ที่มี search match. (6) **Onboard help system** (`a540953`) — เพิ่มปุ่ม "ช่วยเหลือ" / `?` icon ที่ header ของ 3 partials หลัก (myinbox 5-step, sla_dashboard 6-step, finance 6-step) ใช้ RsuTour API + area key แยก · auto-start ครั้งแรก. (7) **Sidebar role-gate fixes** (`0fecb3c` + audit log) — Bug: security group toggle visible ให้ user ที่ไม่มี access_identity แต่ items inside hidden หมด · audit ครบ 13 groups เจอ 2 จุด (security + insurance) · เพิ่ม `$has{Group}Group` pattern · gate insurance 4 main items ด้วย `$hasInsurance` (Dashboard Workbook/Insurance Hub/gold_card_pending/gold_card) · documented pattern ใน `AI/knowledge/sidebar-role-gating.md` + audit log `AI/logs/2026-05-23-sidebar-role-gates.md`
- **EDMS SLA Module ครบ 4 Sprint (2026-05-23, branch `claude/adoring-babbage-anRYh`)** — Service Level Agreement layer สำหรับสารบรรณอิเล็กทรอนิกส์. **Revision 2026-05-23**: ปฏิทินเวลาทำการ + วันหยุดอ้างอิง `sys_clinic_hours` (ปฏิทินคลินิกที่มีอยู่แล้ว) แทน table ของตัวเอง — ลด schema duplication. **Sprint 1** (`c1f6d64`): 2 ตารางใหม่ (`sys_doc_sla_policies`, `sys_doc_sla_events`) + ALTER `sys_doc_routings` 11 columns (policy_id, ack/resolve_deadline_at, sla_state, acknowledged_at, warned_at, breached_at, met_at, paused_at, paused_total_secs) + ALTER `sys_staff` (access_edms_sla_admin, notify_sla_via_line) + ALTER `sys_staff_positions` (is_head). Seed 16 policies (4 doc_type × 4 priority). Backfill: routing เก่าที่ยัง open → attach policy ตาม (doc_type, priority) + mark breached ย้อนหลังถ้าเลย deadline. Helper `includes/edms_sla_helper.php` (compute_deadline business-hours-aware, attach_to_routing, acknowledge/extend/pause/resume/warned/breached/met, event_log). Hook ใน `portal/ajax_edms.php` routing:forward → sla_attach_to_routing() + routing:complete → sla_mark_met() + routing:return → cancel SLA. **Sprint 2** (`40d2ff2`): 2 partials ใหม่ (`sla_dashboard.php` KPI tiles + 12mo bar + by-dept donut + overdue list · `sla_policies.php` matrix CRUD + Portal-Escape modal — เวลาทำการลิงก์ออกไป `clinic_data/hours.php`) + AJAX `portal/ajax_edms_sla.php` (13 endpoints entity+action) + ปรับ `myinbox.php` (KPI warning/breached, 6-tab filter, live countdown) + `detail.php` (SLA timeline + progress bar + ack/pause/resume/extend buttons + override checkbox ใน forward form) + sidebar sub-menu 2 อัน. **Sprint 3** (`c22054e`): cron `cron/edms_sla_tick.php` (1ชม. tick, advisory lock GET_LOCK, 3 phases warning→breach→cleanup) + notify helper `includes/edms_sla_notify.php` (in-app log + LINE push best-effort, escalation ไป superadmin + dept_head ตาม policy) + access flag 7-spot ครบ. Confirmed 7 decisions ใน `AI/scratch/edms-sla-spec.md` (เวลาทำการ=ปฏิทินคลินิก `sys_clinic_hours`, escalation superadmin+dept_head, LINE push default on opt-out per user, cron 1h, backfill auto-attach + mark-breached, pause assignee-only, override auto + checkbox + reason required)
- **LINE Admin Chat Phase 1 MVP (2026-05-21)** — admin ↔ LINE user reply UI ใน portal · ตารางใหม่ `sys_line_chat_messages` auto-migrate · webhook hook 5 บรรทัด log inbound user message ตอน text → `line_chat_log_inbound()` (best-effort, ไม่ break flow) · helper `line_chat_send_admin_reply()` ทำ rate-limit (30/min, 500/day per admin) + anti-impersonation (line_user_id ต้องมี inbound history + format `U[0-9a-f]{32}`) + เรียก `send_line_push()` + log outbound พร้อม push_ok/error · UI partial `portal/_partials/line_chat.php` (LINE-green theme, conversation list filter ทั้งหมด/ต้องตอบ/วันนี้, 200 msgs/convo, pagination 20/page, auto-refresh 30s, dark mode, SweetAlert2 errors) · AJAX `portal/ajax_line_chat.php` entity/action (conversation:list/get + message:send_reply) · sidebar entry ใต้กลุ่ม "สื่อสาร" · gate `access_ai` (TODO ผูก dedicated flag `access_line_chat`). Pre-existing PHI-in-webhook-log issue (`line_webhook_log` ยังเขียน message_text เต็มใน sys_error_log) — flagged for Phase 2 hardening
- **AI Admin Chat Phase 1 MVP (2026-05-21)** — RAG-style chatbot สำหรับ admin · 2 ตารางใหม่ (`sys_admin_chat_threads`, `sys_admin_chat_messages`) auto-migrate ใน `includes/admin_chat_helper.php` · context ดึงจาก `dashboard_resolve_data()` 22 keys (verified) + `clinic_status_helper` · PHI scrub layer drop 25+ field names · Gemini multi-turn ผ่าน `gemini-2.5-flash` (fallback 2.0) · UI 2-column ใน partial `portal/_partials/admin_chat.php` (thread list pagination 20/page + chat messages + DOMPurify-sanitized markdown render + dark mode) · AJAX `portal/ajax_admin_chat.php` (entity/action: thread:list/get/create/archive/delete + message:send) · sidebar entry "ผู้ช่วยข้อมูล" ใน AI Suite · gate `access_ai`. Catalog ของ data source: `AI/knowledge/admin-bot-data-sources.md`. Follow-up post-MVP: soft-delete + structured audit, rate-limit, ARIA, lazy-load marked
- **Nurse position auto-flag (2026-05-21)** — `portal/actions/identity_actions.php` ตอน add/edit `sys_staff_positions` ถ้าชื่อตรงกับ 4 ชื่อพยาบาล (พยาบาลวิชาชีพ / หัวหน้าหอผู้ป่วย / รองหัวหน้าหอผู้ป่วย / พยาบาลหัวหน้าเวร) → force `flags.access_nurse_productivity = 1`. Symmetric — rename ออกจากชื่อพยาบาล → flag = 0. Fail-closed ตอน DB lookup เก่าพัง. Centralize ชื่อพยาบาลเป็น `includes/nurse_positions.php` (constants + `is_nurse_position()` helper) — refactor `nurse_productivity_import.php` + `ajax_nurse_productivity.php` ใช้ตามด้วย กัน drift. Recipe: `AI/knowledge/nurse-roles-canonical.md`
- **AI restoration ครบ Phases A + B + C (2026-05-18/19)** — บูรณะ AI auto-reply ที่ส่งคำตอบ stale ("พรุ่งนี้ที่ 17 พ.ค." ค้าง). **Phase A** (`5752680`): detect time-sensitive *question* → bypass FAQ cache → generate fresh. **Phase B** (`1166275` + polish `ff6d6ac`): per-FAQ `is_time_sensitive` flag + admin UI + RAG-first prompt (chunks ขึ้นเป็น primary, topK 5→8, FAQ KB เป็น fallback templates เท่านั้น) + scanner UI bulk "mark time-sensitive". **Phase C** (`bdc9475`): `sys_ai_answer_cache` (day-bounded, SHA256 key, auto-expire 23:59:59 Asia/Bangkok) + `sys_ai_telemetry` (event log: gemini_call/success/fail · cache_hit/miss · faq_hit · bypass · fallback_used · thumbs_up/down — privacy-safe SHA256 line user hash) + admin endpoints `health_summary` / `telemetry_recent` / `cache_purge`. Stale scanner UI + signed receipt URL ก็ landed ในช่วงเดียวกัน
- **LINE FAQ settings drift fix (2026-05-19, `ca4d2ce`)** — `save_clinic_faq_settings()` แก้เป็น patch-merge (ไม่ overwrite missing keys เป็น default) + audit table `sys_line_faq_settings_audit` (auto-create) + เลิก silent PDOException. Pattern เขียนไว้ที่ `AI/knowledge/settings-singleton-patch-merge.md` สำหรับใช้กับ singleton settings tables อื่น
- **Daily Summary (สรุปงานประจำวัน)** — single-page read-only dashboard รวมข้อมูลจาก 4 module ใน 1 หน้า (Productivity ต่อ dept · Cash Book income/expense + top categories · Stock movement + low-stock alerts · Other: gold card / insurance / asset / docs / schedule วันนี้) · date navigator (‹ › วันนี้) · period delta vs เมื่อวาน · A4 print · 7-spot flag `access_daily_summary` ครบ
- **Nurse Productivity (OPD)** — multi-tenant per `sys_departments` · 3-tab UI (Dashboard/Entry/Settings) · daily/monthly/yearly views · period delta · cross-dept comparison · Excel I/O (PhpSpreadsheet) · A4 print · auto-derive RN/หัวหน้า จาก `sys_nurse_schedule_monthly` เมื่อช่องว่าง · 7-spot access flag `access_nurse_productivity` ครบ
- **Context Persistence** — `hot.md` + `AI/` folder (logs/knowledge/scratch)
- **Finance / Cash Book** ครบ 3 phase (A: search/quick-dates/CSV/bulk · B: chart/donut/period delta · C: recurring/attachments/audit log) + dark mode pass สมบูรณ์
- **Bold & Colorful redesign** — sidebar section accents, rainbow header strip, hover micro-interactions (`--lift` var pattern), `RsuFx` library (counters/tilt/skeleton)
- **e_Borrow shell skin** — portal-matching glassmorphism + finance bridge สำหรับค่าปรับ → Cash Book
- **Inventory shared locations** — `asset/admin/manage_locations.php` ใช้ร่วมกันระหว่าง asset + consumables

### กำลังทำอยู่
- **Walk-in QR ครบ (2026-05-24)** — ระบบ commit แล้ว `5f16c31`; รอ test บน staging
- รอ migration `database/migrations/migrate_edms_sla.php` รันใน production (จะ auto-backfill routing เก่า) · user test SLA dashboard + verify cron tick · ตั้ง crontab `0 * * * * curl -s "URL/cron/edms_sla_tick.php?token=YOUR_TOKEN"` หรือ env `EDMS_SLA_CRON_TOKEN`
- รอ migration `database/migrations/migrate_nurse_productivity.php` รันใน production · user test Nurse Productivity module

### Decision ล่าสุดที่ต้องจำ
- **Portal architecture**: หลัง `2026-05-23` refactor — แต่ละ section เป็นไฟล์แยก `portal/X.php` ใช้ `_layout.php` (layout_start/end). เมนูใหม่ใน sidebar **ต้อง** สร้าง section file ใหม่ ไม่ใช่ใส่ `<div id="section-X">` ใน index.php อีกแล้ว. POST handler ต้องอยู่ใน `_init.php` (global) หรือ section-specific file (ถ้า handler ใหญ่). Backwards-compat redirect ใน `index.php` รับ `?section=X` แล้ว 302 ไป `X.php` — bookmark เก่าทำงาน
- **Sidebar group toggle role-gate**: Group toggle (`.psb-section-toggle`) ต้องมี role gate ที่เป็น OR ของ inner-item gates. ห้าม render group ที่เปิดแล้วเจอ empty list — ใช้ pattern `$has{Group}Group` flag คำนวณใน block role flags (portal/index.php ~line 1175). Common pitfall: `!$registryOnly` แปลว่า "ไม่ใช่ partner ภายนอก" — **ไม่ใช่ proxy for admin role**. Recipe + audit table ครบ: `AI/knowledge/sidebar-role-gating.md` · Audit log: `AI/logs/2026-05-23-sidebar-role-gates.md`
- **EDMS SLA architecture**: clock เริ่มจาก `sys_doc_routings.created_at` ตอน forward → policy lookup ตาม `(doc_type, priority_id)` → คำนวณ deadline ผ่าน business hours **อ้างอิงปฏิทินคลินิก `sys_clinic_hours`** (ผ่าน `get_clinic_hours_for_date()`) — ห้ามสร้างตารางปฏิทินซ้ำ ใช้ของกลาง. `sla_state` enum 7 ค่า: on_track / warning / breached / met / paused / cancelled / none. **Cron 1h interval** กำหนด minimum ack_hours = 2h ใน seed (จะได้ warning window ≥ 24min ก่อน cron tick). **Escalation = ส่งให้ superadmin + dept_head พร้อมกัน** — dept_head lookup จาก `sys_staff_positions.is_head=1` + `sys_staff.department_id`. **Notification = LINE push default on** (toggle ผ่าน `sys_staff.notify_sla_via_line`) + in-app log ผ่าน `sys_doc_logs` (action='sla_warning/breached/escalated'). **Override pattern**: forward modal มี checkbox "กำหนดเวลาเอง" → ปลด lock input ack/resolve datetime + reason required → log event `override` ใน `sys_doc_sla_events`. Pause/resume gate = assignee หรือ superadmin (resume ชดเชย deadline ด้วย elapsed pause time). **⚠️ ก่อน enable SLA ต้องตั้งค่าปฏิทินคลินิกก่อน** (`?section=clinic_data&cd_view=hours`) — ถ้าไม่ตั้ง ทุกวันจะ closed → deadline extend ออกไปจน hit hard cap 365 วัน. Spec ครบ: `AI/scratch/edms-sla-spec.md` · Recipe: `AI/knowledge/edms-sla.md`
- **AI auto-reply architecture**: stored FAQ **answers** จะ stale เร็ว (เพราะมี "พรุ่งนี้/วันที่ NN" ฝังเข้าไป) → ระบบ bypass FAQ cache 2 ชั้น (a) detect time-sensitive *question* keyword (Phase A) (b) admin mark FAQ row `is_time_sensitive=1` (Phase B). ทั้งสองชั้น call `ai_qa_generate_answer()` ตรงเพื่อ generate fresh กับ clinic context ตอนนั้น. **กฎเหล็ก**: เก็บ knowledge (facts) ไม่เก็บ answer ที่มี time reference. ดู `AI/logs/2026-05-19-faq-settings-drift-fix.md` + `includes/ai_qa_helper.php::ai_qa_is_time_sensitive_question()` (PHP) และ mirror ใน `portal/_partials/ai_qa_lab.php::faqIsTimeSensitiveQuestion()` (JS) — sync patterns ทั้ง 2 ฝั่งเสมอเมื่อเพิ่ม keyword
- **Singleton settings tables ต้องใช้ patch-merge**: save handler ห้าม overwrite missing keys เป็น default (จะ silent-clobber). อ่าน row ปัจจุบันก่อน → patch onto. ทุก write ต้อง audit + ห้าม silent `catch (PDOException)`. Recipe เต็ม: `AI/knowledge/settings-singleton-patch-merge.md`
- **Nurse Productivity → Schedule integration**: ตอน save daily ถ้าช่อง RN/หัวหน้าว่าง → derive จาก `sys_nurse_schedule_monthly` + `sys_nurse_schedule_global.nurses_json` แล้ว stamp `rn_source='schedule'`/`head_source='schedule'`. Position mapping: `พยาบาลวิชาชีพ` → RN, `หัวหน้าหอผู้ป่วย`/`รองหัวหน้าหอผู้ป่วย`/`พยาบาลหัวหน้าเวร` → head. Schedule เป็น singleton ใช้ร่วมทุก dept (ตามการตัดสินใจ)
- **Gemini model** ใช้ `gemini-2.5-flash` (primary) เท่านั้น — 1.5/2.0 ใกล้ deprecate
- **Modal pattern** ใช้ Portal-Escape (teleport ไป `<body>` + z-index ≥ 9000) ทุกครั้งที่ modal อาจติด ancestor stacking context — reference: `portal/_partials/gold_card.php`
- **Cross-module finance sync** ห้ามเรียก `portal/ajax_finance.php` ตรงจาก sub-module → ใช้ bridge `e_Borrow/admin/ajax_finance_sync.php` pattern เสมอ
- **Dark mode** — surface ที่ใช้ raw `background:#fff` ต้องมี override ที่ `body[data-theme='dark']` (portal) หรือ `body.dark-mode` (e_Borrow); utility class `.bg-white` มี global override แล้ว

### ข้อห้าม / ของอันตราย
- ห้ามใช้ arbitrary Tailwind values (`z-[200]`, `max-h-[90vh]`) — ไม่ compile · ใช้ `<style>` block แทน
- ห้ามใช้ `alert()/confirm()/prompt()` — ใช้ `Swal.fire()` (SweetAlert2 โหลดอยู่ที่ `portal/index.php` line ~540)
- ห้ามใส่เมนูลอยใน portal sidebar — ต้องอยู่ใน 12 section ที่กำหนด (ดู CLAUDE.md "Portal Sidebar")
- ห้าม commit secrets — `config/secrets.php` อยู่ใน `.gitignore`
- ตารางข้อมูลทุกตัวต้องมี pagination 20/หน้า + ปุ่ม `« ‹ … › »`

---

## Quick Reference (paths ที่เปิดบ่อย)

| ต้องการ | ดูที่ |
|---|---|
| Portal shell + section routing | `portal/index.php` |
| Portal partial ใหม่ | `portal/_partials/<name>.php` |
| Portal AJAX กลาง | `portal/ajax_clinic_master.php` (entity+action) |
| Finance AJAX | `portal/ajax_finance.php` |
| Cross-module finance sync | `includes/finance_sync_helper.php::finance_sync_upsert()` |
| Design tokens / components | `assets/STYLE_GUIDE.md` + `tailwind.config.js` |
| Tailwind source (rebuild!) | `assets/css/tailwind.src.css` → `npm run build:css` |
| FX library (counter/tilt/skeleton) | `assets/js/rsu-fx.js` (`window.RsuFx`) |
| Guided tour | `assets/js/rsu-tour.js` (`RsuTour.maybeAutoStart(key, steps)`) |
| Access flags / roles | CLAUDE.md → "Permissions / Access Flags" (7-spot checklist) |
| LINE webhook | `api/line_webhook.php` |
| Doctor schedule | `sys_doctor_schedule` (regular/override/off) |
| Asset locations (shared) | `asset/admin/manage_locations.php` |
| e_Borrow shell | `e_Borrow/includes/header.php` + `e_Borrow/assets/css/eb-skin.css` |

---

## Workflow recap
1. อ่าน `hot.md` (ไฟล์นี้) + `CLAUDE.md` ทุก session
2. ก่อนสร้างของใหม่ — เช็คว่ามี token / component / helper เดิมรองรับอยู่ไหม
3. หลัง commit ใหญ่ → อัพเดต `hot.md` (Phase / Decision / กำลังทำ) + drop note ใน `AI/logs/YYYY-MM-DD-<topic>.md` ถ้า decision สำคัญ
4. ของชั่วคราว / draft → `AI/scratch/` (จะถูก clean เป็นระยะ)
5. Distilled knowledge ถาวร → `AI/knowledge/` (เช่น schema notes, troubleshooting recipes)

ดู `AI/README.md` สำหรับ convention เต็ม
