<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <system.webServer>
        <rewrite>
            <rules>
                <!--
                    DEBUG: ทดสอบว่า URL Rewrite ทำงานบนนี้หรือเปล่า
                    Visit: https://healthycampus.rsu.ac.th/e-campaignv2/test-rewrite-works
                    - ถ้าเห็นหน้า 404 สวย → URL Rewrite ทำงาน
                    - ถ้าเห็น IIS Server Error → URL Rewrite ถูก Plesk ล็อก
                -->
                <rule name="DebugRewriteTest" stopProcessing="true">
                    <match url="^test-rewrite-works$" />
                    <action type="Rewrite" url="/e-campaignv2/errors/404.php" />
                </rule>

                <!-- Redirect Legacy e_Borrow Login -->
                <rule name="RedirectLegacyLogin" stopProcessing="true">
                    <match url="^archive/e_Borrow/login\.php$" />
                    <action type="Redirect" url="e_Borrow/login.php" redirectType="Permanent" />
                </rule>

                <!-- Force HTTPS -->
                <rule name="ForceHTTPS" stopProcessing="true">
                    <match url="(.*)" />
                    <conditions>
                        <add input="{HTTPS}" pattern="off" ignoreCase="true" />
                    </conditions>
                    <action type="Redirect" url="https://{HTTP_HOST}/{R:1}" redirectType="Permanent" />
                </rule>

                <!--
                    Catch-all 404 — rewrite missing-file requests to errors/404.php
                    Uses absolute path in action so it works whether e-campaignv2
                    is a sub-application or just a virtual directory.
                -->
                <rule name="CustomNotFound" stopProcessing="true">
                    <match url=".*" />
                    <conditions logicalGrouping="MatchAll">
                        <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
                        <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
                        <add input="{REQUEST_URI}" pattern="^/e-campaignv2/errors/" negate="true" />
                    </conditions>
                    <action type="Rewrite" url="/e-campaignv2/errors/404.php" />
                </rule>
            </rules>
        </rewrite>

        <!--
            Custom error pages (kept as fallback — fires for errors raised AFTER
            URL Rewrite, e.g. PHP fatal -> 500, denied auth -> 403)
        -->
        <httpErrors errorMode="Custom" existingResponse="Replace">
            <remove statusCode="400" />
            <remove statusCode="403" />
            <remove statusCode="404" />
            <remove statusCode="500" />
            <remove statusCode="502" />
            <remove statusCode="503" />
            <remove statusCode="504" />
            <error statusCode="400" path="/e-campaignv2/errors/404.php" responseMode="ExecuteURL" />
            <error statusCode="403" path="/e-campaignv2/errors/403.php" responseMode="ExecuteURL" />
            <error statusCode="404" path="/e-campaignv2/errors/404.php" responseMode="ExecuteURL" />
            <error statusCode="500" path="/e-campaignv2/errors/500.php" responseMode="ExecuteURL" />
            <error statusCode="502" path="/e-campaignv2/errors/500.php" responseMode="ExecuteURL" />
            <error statusCode="503" path="/e-campaignv2/errors/503.php" responseMode="ExecuteURL" />
            <error statusCode="504" path="/e-campaignv2/errors/503.php" responseMode="ExecuteURL" />
        </httpErrors>

        <!-- Security headers -->
        <httpProtocol>
            <customHeaders>
                <add name="X-Frame-Options" value="SAMEORIGIN" />
                <add name="X-Content-Type-Options" value="nosniff" />
                <add name="Referrer-Policy" value="strict-origin-when-cross-origin" />
            </customHeaders>
        </httpProtocol>

        <!-- Default document -->
        <defaultDocument>
            <files>
                <clear />
                <add value="index.php" />
                <add value="index.html" />
            </files>
        </defaultDocument>
    </system.webServer>
</configuration>
