# Deny all direct web access to AI/
# Memory store for AI agents (logs, knowledge, scratch) — server-side only.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Deny,Allow
    Deny from all
</IfModule>
